let showworking<br>smoothly = false;

function changeVisibilityOfworking<br>smoothly() {
    showworking<br>smoothly = !showworking<br>smoothly;
    if (showworking<br>smoothly) {
        document.getElementById("showworking<br>smoothly").style["display"] = "none";
    } else {
        document.getElementById("showworking<br>smoothly").style["display"] = "block";
    }
}
